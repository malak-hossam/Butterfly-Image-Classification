base_config: default.yaml

project:
  pipeline_name: cnn

model:
  name: cnn
  pretrained: false
  dropout: 0.5

training:
  epochs: 10
  learning_rate: 0.0001
  weight_decay: 0.0
  use_class_weights: true
  freeze_backbone: false
